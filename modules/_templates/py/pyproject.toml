[project]
name = "project_name"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "example", email = "example@proton.me" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
py = "project_name:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
